# Annotated line story example

This example creates a line chart with a d3-style annotation (subject, connector, note).

Run:

```bash
PYTHONPATH=../../src python3 annotated_line_story.py
```

Output:
- `annotated_line_story.html`
