# Threshold line story example

This example shows a line chart with a horizontal threshold line for a KPI target.

Run:

```bash
PYTHONPATH=../../src python3 threshold_line_story.py
```

Output:
- `threshold_line_story.html`
